export const metadata = {
  title: 'Resources & Datasets',
  description: 'Access biotech research datasets, market reports, and analysis tools',
};

export default function ResourcesLayout({ children }) {
  return children;
}
